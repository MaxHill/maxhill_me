# Build-time env for `pnpm exec vite build` in vps/golf/deploy.sh.
# Public URLs only — no secrets, committed to the repo.
# Vite inlines VITE_* vars into the built bundle.
VITE_AUTH_URL=https://auth.maxhill.me
VITE_SYNC_URL=https://sync.maxhill.me/sync
