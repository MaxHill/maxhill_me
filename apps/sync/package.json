{
  "name": "sync",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "opam exec --switch=. -- dune build",
    "dev": "opam exec --switch=. -- dune exec ./bin/main.exe -- --auth-issuer-url http://localhost:3002 --auth-audience golf-app",
    "test": "opam exec --switch=. -- dune runtest",
    "sim:search": "opam exec --switch=. -- dune build ./sim_bin/simulator_sut.exe && opam exec --switch=. -- dune exec ./sim_bin/simulator.exe -- search",
    "sim:replay": "opam exec --switch=. -- dune build ./sim_bin/simulator_sut.exe && opam exec --switch=. -- dune exec ./sim_bin/simulator.exe -- replay",
    "sim:sut": "opam exec --switch=. -- dune exec ./sim_bin/simulator_sut.exe --",
    "sim:client": "node ./sim/test_client.js"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  },
  "dependencies": {
    "fake-indexeddb": "^6.2.5"
  }
}
