import CodeExample from '../../../../../apps/site/src/components/CodeExample.astro';

A form-associated custom element that behaves like a native `<input>` with support for validation, clearable state, slots, and CSS parts for styling.

<CodeExample html={`<div class="form">
  <m-input 
    label="Your name" 
    name="username" 
    placeholder="Type here…"
  ></m-input>
</div>`} />
