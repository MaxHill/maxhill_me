| Key | Action |
|-----|--------|
| `Tab` | Move focus to/from the input |
| `Escape` | Clear the input (when clearable and focused) |
| `Enter` | Submit the parent form |
| `Ctrl+A` / `Cmd+A` | Select all text in the input |
