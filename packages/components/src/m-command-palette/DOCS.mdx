## Examples

### Basic Usage

A simple m-command-palette example.

<m-command-palette example="test">
  Default content
</m-command-palette>
```html
<m-command-palette example="test">
  Default content
</m-command-palette>
```

## Attributes

- **`example`** (string): An example property

## Slots

- **default**: Default slot for component content

## Events

- **`m-command-palette-change`**: Fired when the example changes. Detail: `{ example: string }`

