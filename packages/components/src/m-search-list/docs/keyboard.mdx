Keyboard interaction is handled through the slotted `<input>` element. The component itself adds no additional keyboard bindings.

| Key | Action |
|-----|--------|
| Any character | Types into the search input, triggering filtering |
| Escape | Clears the search input (native search input behavior) |
