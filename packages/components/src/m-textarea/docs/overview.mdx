import CodeExample from '../../../../../apps/site/src/components/CodeExample.astro';

A form-associated custom element textarea that behaves like a native `<textarea>` with added features: clearable content, validation states, slotted content, and CSS parts for styling.

<CodeExample
  html={`<form class="form">
  <m-textarea
    label="Message"
    name="message"
    placeholder="Enter your message..."
    rows="4"
  ></m-textarea>
</form>`}
/>
