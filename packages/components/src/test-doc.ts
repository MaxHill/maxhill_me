import { MElement } from "./utils/m-element";

/**
 * A test component to verify JSDoc parsing.
 * 
 * @customElement
 * @tagname test-doc
 */
export class TestDoc extends MElement {
    static tagName = 'test-doc';
}
