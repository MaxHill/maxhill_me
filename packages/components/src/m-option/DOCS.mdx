## Overview

A selectable option within list-based components (`<m-listbox>`, `<m-combobox>`, etc). Represents a single option that can be selected, focused, and disabled.

## Attributes

- **`value`** (string, required): The value to submit with the form when this item is selected
- **`selected`** (boolean): Whether this item is initially selected
- **`disabled`** (boolean): Whether this item is disabled and cannot be selected

## Slots

- **default**: Item content/label

## Example

```html
<m-listbox name="fruit">
  <m-option value="apple">Apple</m-option>
  <m-option value="pear" selected>Pear</m-option>
  <m-option value="orange" disabled>Orange (out of stock)</m-option>
</m-listbox>
```

## See Also

- [`<m-listbox>`](/components/m-listbox) - Container that manages listbox items
