(include_subdirs unqualified)

(test
 (name test_{{projectName}})
 (libraries {{projectName}} hegel compiler-libs.common)
 (preprocess (pps ppx_hegel_test))
 (deps
  (glob_files ../lib/*.ml)
  (glob_files ../bin/*.ml))
 (action (run %{test} %{deps})))
